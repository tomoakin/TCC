import(ROC)
import(DESeq)
import(baySeq)
import(edgeR)
import(affy)
import(som)


# Export TCC class.
exportClasses(
    "TCC" 
)

# Export the public methods in TCC package.
export(
  show,
  names,
  length,
  "[",
  TCC,
  calcAUCValue,
  calcNormFactors,
  do_TbT,
  estimateDE,
  exactTestafterTbT,
  filterLowCountGenes,
  simulateReadCounts,
  getNormalizedData,
  getResult,
  MAplot,
  NBsample,
  plotFCPseudocolor,
  plot.TCC,
  WAD,
  ROKU
)

